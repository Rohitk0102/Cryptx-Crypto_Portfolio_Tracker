Never modify config files without explicit user approval.

Never create unwanted files of any kind.

Never create empty files.

Never generate placeholder or boilerplate files unless specifically requested.

Never create test files unless meaningful tests are required.

Never generate multiple files for small or trivial implementations.

Never install unnecessary dependencies.

Never create unused folders or unnecessary nested directory structures.

Never change coding style beyond the project’s existing conventions.

Never violate the project’s architecture pattern or folder structure.

Never mix layers (UI, service, database, domain).

Never leak business logic into controllers or routing layers.

Never generate low-value or shallow tests just to increase file count.

Never swallow errors or create silent failures.

Never produce inconsistent naming or formatting.